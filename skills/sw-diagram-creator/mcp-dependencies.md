# MCP Dependencies – sw-diagram-creator

**No MCP servers required.**

This skill generates diagrams purely from text input and Claude's reasoning. No external tools, rendering APIs, databases, or MCP calls are needed or used. Diagram code is output as plain text in fenced code blocks — rendering happens in the user's editor, Notion, GitHub, or any Mermaid/PlantUML-compatible viewer.
