# MCP Dependencies – sw-use-case-coder

No MCP servers required.

This skill uses only:
- File system reads (`requirements/tech-stack.yaml`, `requirements/use-cases.md`, `requirements/user-stories.md`)
- File system writes (`requirements/code/<uc-slug>/`)

All file operations are handled natively by Claude Code.
