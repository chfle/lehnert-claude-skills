# MCP Dependencies – sw-user-story-creator

**No MCP servers required.**

This skill reads local files (`requirements/vision.md`) and generates Markdown output entirely from Claude's reasoning. No external tools, APIs, or MCP calls are needed.
