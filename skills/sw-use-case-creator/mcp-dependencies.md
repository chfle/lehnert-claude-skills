# MCP Dependencies – sw-use-case-creator

**No MCP servers required.**

This skill reads local files (`requirements/user-stories.md`, `requirements/vision.md`) and generates Markdown output entirely from Claude's reasoning. No external tools, APIs, or MCP calls are needed.
